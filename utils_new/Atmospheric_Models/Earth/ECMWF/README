The coefficients in the .dat file come directly from ECMWF's L137 model definition (https://www.ecmwf.int/en/forecasts/documentation-and-support/137-model-levels).

The Python script is here to ease server requests, but needs API configuration.

The Matlab script is here to help building server requests (using the Python script) and API configuration.
